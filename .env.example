# Environment Configuration Template

# Todoist API Token
# Get your token from: https://todoist.com/prefs/integrations
# Look for "API token" section and copy the token
TODOIST_API_TOKEN=your_todoist_api_token_here

# Gmail API (OAuth2 - no token needed in .env)
# Authentication handled via OAuth2 flow
# Required files (git-ignored):
#   - local_data/gmail_credentials.json (download from Google Cloud Console)
#   - local_data/gmail_token.json (auto-generated on first run)
# Setup:
#   1. Enable Gmail API in Google Cloud Console
#   2. Download OAuth credentials
#   3. Save as local_data/gmail_credentials.json
#   4. Run process_emails.py to authenticate

# Google Calendar API (OAuth2 - no token needed in .env)
# Same OAuth credentials can be used for both Gmail and Calendar
# Required files (git-ignored):
#   - local_data/calendar_credentials.json
#   - local_data/calendar_token.json (auto-generated)

# Anthropic API Key (for Email Digest System - Phase 2.5)
# Get your API key from: https://console.anthropic.com/
# Required for: biweekly_email_digest.py
# Features:
#   - AI-powered email interest analysis
#   - Smart digest generation (urgent/high/medium/low)
#   - Learning system with feedback tracking
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional Claude API Configuration
# Model: claude-sonnet-4-20250514 (default)
# CLAUDE_MODEL=claude-sonnet-4-20250514
# CLAUDE_MAX_TOKENS=2000
# CLAUDE_TEMPERATURE=0.3

# Optional: Add other configuration as needed
# DEFAULT_PROJECT=Work
