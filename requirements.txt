# Core dependencies for Todoist + Claude Integration
requests>=2.31.0
python-dotenv>=1.0.0

# Google API Integration (Calendar + Gmail)
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.0.0

# Email Processing
beautifulsoup4>=4.12.0
lxml>=4.9.3

# Fix for macOS LibreSSL compatibility
# urllib3 v1.x is compatible with LibreSSL, v2.x requires OpenSSL 1.1.1+
urllib3<2.0.0